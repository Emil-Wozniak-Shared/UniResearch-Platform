export type DashboardStats = {
  researchersCount: number;
  institutionsCount: number;
  activeGrantsCount: number;
  publicationsThisYear: number;
}

export type GrantFundingByYear = {
  year: number;
  totalAmount: number;
}
